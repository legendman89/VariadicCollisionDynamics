{
  // Display name shown in logs and debug UI.
  "name": "Camera Dialogue",

  // Position offset: [side, forward, unused z]. Keep z at 0.0.
  "position": [-5.0, 30.0, 0.0],

  // Collision radius.
  "radius": 6.0,

  // First endpoint: [x, y, z].
  "firstPoint": [0.0, 0.0, 0.0],

  // Second endpoint: [x, y, z].
  "secondPoint": [0.0, 0.0, 0.0]
}
